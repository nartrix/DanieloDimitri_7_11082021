# DanieloDimitri_7_11082021

### Backend ###

Utilisé la commande `npm install`.

Lancer le serveur avec `nodemon server`.

### Frontend ###

Utilisé la commande `npm install`.

Lancer l'application avec `npm start`.
